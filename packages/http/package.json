{
  "name": "@neige/http",
  "version": "0.1.0",
  "description": "A multithread http server for nodejs write in Rust.",
  "type": "module",
  "main": "build/",
  "scripts": {
    "build": "cargo build --release && tsc",
    "dev": "cargo build && tsc -w",
    "lint": "cargo clippy && eslint src/**/**.ts",
    "check": "cargo check && tsc --noEmit",
    "test": "cargo test",
    "clean": "rm -rf build; cargo clean"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "koffi": "^2.6.12"
  }
}
